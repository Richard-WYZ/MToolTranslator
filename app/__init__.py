"""Application layer for MToolTranslator.

This package owns HTTP/Desktop concerns and delegates translation behavior to
the translation layer. The application composition root lives in ``app.main``.
"""
